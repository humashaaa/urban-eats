import { Helmet } from "react-helmet-async";
import { Link } from "react-router-dom";

const Ourmenu = () => {
    return (
        <div>
             <Helmet>
                <title>Menu</title>
             </Helmet>
        </div>
    );
};

export default Ourmenu;